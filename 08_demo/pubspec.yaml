name: clotho_ui_demo
description: Clotho Presentation Layer UI Demo
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.4.9
  # 依赖注入
  get_it: ^7.6.0
  injectable: ^2.3.0
  # 网络层
  dio: ^5.4.0
  # 本地存储
  sqflite: ^2.3.0
  path: ^1.8.3
  uuid: ^4.3.0
  # Filament 解析器
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  # 代码生成
  build_runner: ^2.4.0
  injectable_generator: ^2.4.0
  # 测试框架
  mockito: ^5.4.0
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
